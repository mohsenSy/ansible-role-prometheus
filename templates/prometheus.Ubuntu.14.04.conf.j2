# Run prometheus

start on startup
console log

script
   "/opt/Prometheus/prometheus-1.7.1.linux-amd64/prometheus" "--config.file" "/opt/Prometheus/prometheus-1.7.1.linux-amd64/prometheus.yml" "--web.listen-address=0.0.0.0:{{ PROMETHEUS_BIND_PORT }}"
end script

post-stop script
  sleep 1
end script
