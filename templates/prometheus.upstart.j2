# Run prometheus

start on startup

script
{{ prometheus_dir }}/prometheus-{{ prometheus_version }}.linux-amd64/prometheus --config.file={{ prometheus_dir }}/prometheus-{{ prometheus_version }}.linux-amd64/prometheus.yml
end script
